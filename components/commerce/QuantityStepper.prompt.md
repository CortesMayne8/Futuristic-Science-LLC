Minus / value / plus quantity control for cart and product pages.

```jsx
<QuantityStepper value={qty} onChange={setQty} />
```

- Hairline bordered group, mono value, clamps to `min`/`max`. `size="sm"` for cart line items.
